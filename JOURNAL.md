# Журнал испытаний Lunagram для iOS

> Записи идут по порядку. Время — местное, GMT+5.
>
> **Как читать номера.** Версия — это то, что собралось и работает.
> - **1.x** — переносим на айфон то, что уже есть на ПК. Каждая функция —
>   следующая цифра: 1.1, 1.2, 1.3…
> - **Третья цифра** — исправления без новых функций: 1.2.1, 1.2.2.
> - **2.0** — всё с ПК перенесено и проверено на живом телефоне. Дальше
>   2.x — новое, чего нет даже на ПК.
>
> Между версиями — **испытания**: неудачные сборки тоже записаны, с
> настоящей причиной и выводом.

---

## Предыстория

До iOS было три захода, все на ПК, все сохранены:

- **Свой клиент на Swift + TDLib** (август) — собирался, но выглядел
  самодельным. Отложен.
- **Свой клиент на Electron** — работал, Ghost и архив удалённых
  проверены на живом аккаунте. Отложен: не похож на Telegram.
- **Форк Telegram Web K** — Ghost встроен в их код, проверен вживую.
  Отложен: интерфейс веб-версии, а хотелось настольный.
- **Форк AyuGram Desktop (C++)** — основной. Переименован в Lunagram,
  поверх написано около 8600 строк своего: живые обои на всё окно,
  баннер и рамка профиля, плитки в списке, частицы, матовое стекло.

Решено: iOS-версия должна быть такой же. Своего AyuGram под iOS не
существует — значит всё наше, включая Ghost, пишется с нуля.

---

## Разведка · 9 сентября

Изучен файл приятеля, «Jerkgram». Оказалось — официальный Telegram
12.9.2 с переименованным заголовком и двумя служебными библиотеками от
программы установки (`sideloadKeychainFix`, `FilePickerFix`). Своего
кода — ноль.

**Вывод:** путь установки на телефон рабочий. Но чтобы добавить своё,
нужна настоящая сборка из исходников, а не переименование.

Скачаны исходники Telegram для iOS, тег `release-12.9.2`. Требования:
macOS 26, Xcode 26.2, Bazel 8.4.2. Своего Mac нет — собирать будем на
машинах GitHub.

---

## Испытание 1 · к версии 1.0 · 9 сентября, 22:07 — неудача

**Задача:** собрать чистый Telegram из исходников.

Компиляция прошла вся — 35 минут. Упало на самом последнем шаге:

```
Unable to find an identity on the system matching the ones in
.../provisioning/Intents.mobileprovision
```

**Причина:** профили подписи в исходниках есть, а сертификата под них
на чистой машине нет.
**Вывод:** рядом с профилями Telegram кладёт `SelfSigned.p12` ровно для
этого. Надо положить его в связку ключей перед сборкой.

## Испытание 2 · к версии 1.0 · 9 сентября, 23:07 — неудача

Сертификат помог — сборка прошла целиком, 1 час 10 минут. Упал мой шаг
переименования: готовый файл я искал обходом папок, а Bazel кладёт его
за символической ссылкой, куда обход по умолчанию не заходит.

**Попутный провал:** кэш не сохранился — работа упала, шаг сохранения
пропустился, час компиляции ушёл впустую.
**Вывод:** у сборщика Telegram есть `--outputBuildArtifactsPath` —
просим положить готовое куда надо. Кэш сохраняем при любом исходе.

## Испытание 3 · к версии 1.0 · 10 сентября, 09:33 — неудача

Упало за две минуты. На этот раз не мы:

```
Download from https://github.com/nlohmann/json/.../include.zip failed:
GET returned 500 Internal Server Error
```

**Причина:** сбой на стороне самого GitHub.
**Вывод:** перезапуск.

---

## Версия 1.0 · 10 сентября, 09:40 — работает

**Сборка №4. 1 час 3 минуты. Первый готовый `Lunagram.ipa`.**

Чистый Telegram, собранный из исходников, с нашими ключами API и именем
Lunagram. Своих функций пока нет — задача была доказать, что вся цепочка
работает: машина, исходники, подпись, упаковка.

С этой сборки записался кэш.

---

## Полевые испытания: установка · 10 сентября — отложено

**Задача:** поставить версию 1.0 на телефон через Sideloadly.

**Препятствие 1 — компьютер не видел айфон.** Спрашивал только «доступ
к фото», а не «доверять этому компьютеру». Разобрано: Windows подцепила
весь айфон к стандартному драйверу фотоаппарата (MTP), а служебный вход,
через который работают iTunes и Sideloadly, остался без драйвера.
Причина: **свежий iTunes 12.13 больше не везёт драйвер Apple в
установщике** — распаковал и проверил, внутри только языковые файлы.
Решено приложением «Устройства Apple» из Microsoft Store: оно ставит
драйвер при первом запуске.

**Препятствие 2 — Apple не пустил новую учётную запись.** Для подписи
заведён отдельный Apple ID, чтобы не отдавать основной в стороннюю
программу. Вход с компьютера отклонён и в Sideloadly (`-22410`), и в
самом iTunes — «Не удалось выполнить действие». Пароль верный: на сайт
Apple под ним пускает. Apple придерживает только что созданные учётные
записи для входа из программ.

**Вывод:** ждём, пока учётная запись «оттает».

---

## Версия 1.1 · 10 сентября — работает

**Сборка №5. 28 минут. С первой попытки.**

Первая заплатка со своим кодом, `0001`:

- **Модуль `Lunagram`** — отдельный, зависит только от системных
  библиотек. Всё наше будет жить в нём, а в код Telegram вставлены только
  «крючки» по нескольку строк. При обновлении Telegram ломаться будут
  крючки, а не наш код.
- **Раздел «Lunagram»** в настройках — по образцу экрана Apple Watch.
- **Частицы в переписке** — рисует системный `CAEmitterLayer` на
  видеокарте, без таймеров. Встают поверх обоев, но под сообщения.

Кэш сработал: полчаса вместо часа.

**Главное, что доказано:** наш модуль правильно подключается к чужим
частям приложения. Дальше все функции пойдут по этой колее.

**Заодно в сборке:** запускается сама при новой заплатке; при падении
выписывает ошибки компилятора пометками, которые видно без скачивания
журнала.

---

## Версия 1.2 · 10 сентября — работает

**Сборка №6. 54 минуты. С первой попытки.**

Дольше, чем 1.1, и это ожидаемо: правки легли в `TelegramCore` — сетевое
сердце Telegram, от которого зависит почти всё приложение. Тронул его —
пересобирается большая часть.

Заплатка `0002`, **Ghost**. Четыре отдельных выключателя:

| Выключатель | Где стоит крючок |
|---|---|
| Не отмечать прочитанным | `SynchronizePeerReadState`, плюс прослушивание голосовых в `ManagedConsumePersonalMessagesActions` |
| Не показывать «печатает…» | `ManagedLocalInputActivities` |
| Не выходить в сеть | `ManagedAccountPresence` |
| Не отмечать истории | `ManagedSynchronizeViewStoriesOperations`, `Stories` |

**Самое тонкое место — сверка прочтения.** Telegram периодически
спрашивает сервер, сколько непрочитанного. При Ghost сервер отстаёт от
нас нарочно. Без правки приложение либо гоняло бы запросы по кругу,
либо откатывало бы своё к серверному — и сообщения снова становились бы
непрочитанными у самого человека. Поправлено: при Ghost расхождение
считается нормой.

**Известное ограничение:** «Отметить всё прочитанным» из списка
переписок пока не заглушено — это прямое действие человека.

---

## Версия 1.3 · 10 сентября — работает

**Сборка №7. 43 минуты. С первой попытки.**

Заплатка `0003`, **живые обои**: видео вместо обоев переписки.

- В разделе «Lunagram» — выключатель, кнопка «Выбрать видео» и три
  степени затемнения. Выбрал ролик — обои включаются сами.
- Ролик копируется к приложению: галерея отдаёт временный файл, который
  система удаляет сразу после выбора.

**Уроки с ПК, учтённые сразу:**

- **Один проигрыватель на всё приложение.** На ПК каждая часть окна
  открывала ролик сама, 4K раскодировалось по нескольку раз за кадр — всё
  тормозило. Здесь раскодирование одно, а каждый фон переписки лишь
  показывает общую картинку своим слоем.
- **Нет фонов на экране — видео на паузе.** Батарея важнее.

**Новое, чего на ПК не было:**

- **Звуковая дорожка вырезается целиком.** Приглушить мало: даже
  беззвучный системный проигрыватель с дорожкой может оборвать музыку в
  другом приложении.
- **Телефону не мешаем засыпать** — видео по кругу иначе держало бы экран.

**Подводные камни, обойдённые заранее:**

- Минимальная iOS у Telegram — 13, а галерея `PHPicker` появилась в 14-й.
  Вызывается с проверкой версии.
- Наш модуль входит в сетевую библиотеку Telegram, а её подключают и
  расширения, где `UIApplication` запрещён. Уведомление о возвращении
  приложения ловим по имени-строке, а не через `UIApplication`.

**Почему 43 минуты, а не 28, как у 1.1.** Ghost живёт в модуле
`Lunagram`, а на нём теперь держится `TelegramCore`. Добавил в модуль
новый класс — пересобирается сетевое сердце, а за ним почти всё
приложение. **Вывод:** выключатели Ghost стоит вынести в отдельный
крошечный модуль, который меняется редко. Тогда новые функции будут
собираться за полчаса.

---

## Версия 1.4 · 10 сентября — работает

**Сборка №8. 6 минут. С первой попытки.**

Самая быстрая сборка: код не менялся, только картинка значка. Всё
остальное взято из кэша — он работает как задумано.

Заплатка `0004`, **значок**. Самолётик Telegram заменён луной с ПК.

- Геометрия та же, что у настольного значка, — луна минус «надкус»,
  только вдвое крупнее: на айфоне холст 1024 точки, а не 512.
- Синий круг стал ночным фиолетовым, как на ПК: сверху `#3A3470`, книзу
  `#16142A`. Для тёмной темы — светлее, `#5A53BF`, иначе круг сольётся
  с чёрным фоном.
- Стекло, блики и тени iOS 26 накладывает сама — в файле только форма и
  цвет.

**Как устроено.** С iOS 26 значок — это не набор картинок, а пакет
`Telegram.icon` из программы Icon Composer: слои в SVG плюс описание,
как их красить и остеклять. Имена слоёв оставлены телеграмовскими
(`Plane`, `Oval`) — поменялось только содержимое. Так описание значка
почти не тронуто: ошибка в его устройстве уронила бы сборку на этапе
значков, а там сообщения об ошибках самые невнятные.

**Проверено до сборки:** превью нарисовано на ПК тем же расчётом, что и
слой, — чтобы не ждать сборку ради проверки формы и цвета.

**Чего нет:** сменные значки Telegram из настроек оформления остались
прежними. Основной — наш.

---

## Версия 1.4.1 · 10 сентября — работает

**Сборка №9, вместе с 1.5. 38 минут. С первой попытки.**

Меньше, чем у 1.2 и 1.3, хотя `TelegramCore` пересобирался: кэш
подхватил всё, что от него не зависит. Настоящий выигрыш покажет
следующая функция.

Заплатка `0005`, **Ghost переехал в свой модуль**. Функций не прибавилось —
это ремонт мастерской, вывод из 1.3.

- Новый модуль `LunagramGhost` — четыре выключателя и больше ничего.
  `TelegramCore` теперь зависит только от него, а не от всего Lunagram.
- Раздел настроек пишет туда же, что и раньше, — ключи те же. Включённый
  Ghost после обновления остаётся включённым.

**Как проверим:** эта сборка сама по себе будет долгой — `TelegramCore`
пересобирается, надеюсь, в последний раз. Выигрыш покажет следующая
функция.

---

## Версия 1.5 · 10 сентября — работает

**Сборка №9, та же. С первой попытки.**

Заплатка `0006`, **баннер профиля**: фото или ролик в шапке своего
профиля. Собирается той же сборкой, что и 1.4.1.

- В разделе «Lunagram» — выключатель, «Выбрать фото или видео» и четыре
  степени затемнения. По умолчанию чуть больше половины — как на ПК.
- Виден только тебе, как и на ПК: это оформление твоего приложения, на
  сервер оно не уходит.

**Главная хитрость — как встроено.** Шапку профиля у Telegram рисует файл
почти на три тысячи строк. Чтобы имя, кнопки и строка состояния читались
на картинке, их пришлось бы перекрашивать в десятке мест. Вместо этого
шапке говорим: «у этого профиля есть цвет» — как у премиум-профилей.
Дальше Telegram всё делает сам: белое имя, белые кнопки, светлая строка
состояния, цветная подложка. А баннер ложится поверх подложки. Итого в
чужом файле — 28 строк, и все они наши вставки, ни одной переделанной.

**Как ведёт себя:** потянул шапку вниз — баннер растягивается вместе с
ней; листаешь вверх — уезжает вместе со списком, не сжимаясь.

**Уроки с ПК, учтённые сразу:**

- **Фото уменьшается при выборе** — до 2048 точек по длинной стороне,
  в JPEG. На ПК тяжёлые файлы тормозили всё окно; снимок с камеры в памяти
  занимает до двухсот мегабайт, а шапке столько не нужно.
- **Затемнение ровное, на всю площадь.** Градиент снизу на ПК пробовали —
  до имени он не доставал.
- **Ролик на паузе, когда профиля не видно.** Экран настроек у Telegram
  живёт всё время, пока открыто приложение, — без этого видео крутилось бы,
  пока ты сидишь в переписках.

**Заодно:** проигрыватель живых обоев стал общим устройством — у обоев и
у баннера теперь по своему экземпляру. Ролики у них разные, и с одним на
двоих каждый переход из переписки в профиль открывал бы файл заново.

**Чего пока нет из ПК:** десяти нарисованных баннеров, подгонки кадра
(сдвиг и увеличение), размытия, рамки вокруг аватарки и фона под списком
профиля. Пойдут следующими номерами.

---

## Версия 1.6 · 10 сентября — работает

**Сборка №10. 29 минут. С первой попытки.**

Разделение из 1.4.1 окупилось: функции 1.2 и 1.3 собирались 54 и
43 минуты, эта — 29, хотя список переписок у Telegram из самых больших
частей приложения. `TelegramCore` больше не пересобирается.

Заплатка `0007`, **плитки в списке переписок**: фон каждой строки — её
размытая аватарка.

- В разделе «Lunagram» — выключатель, «Приглушённые бледнее» и три
  степени подложки.
- Плитка скруглённая, с отступом от краёв и тонкой обводкой, которая тает
  книзу, — как на ПК.

**Отличие от ПК — подложка цвета темы.** На ПК поверх аватарки всегда
ложилось затемнение, и в светлой теме чёрный текст на тёмной плитке
потерялся бы. Здесь подложка — цвет фона темы: в тёмной это то же
затемнение, в светлой — высветление.

**Особенности iOS, учтённые сразу:**

- **Плитка уезжает вместе со строкой при свайпе** — к кнопкам «Архив»,
  «Удалить». Она лежит внутри той части строки, что сдвигается, а не под
  ней.
- **Нажатие видно.** Подсветку нажатия Telegram рисует под строкой, и
  непрозрачная плитка её закрыла бы. При нажатии плитка становится
  полупрозрачной — подсветка проступает сквозь аватарку.
- **Разделители между строками прячутся** — между плитками и так просвет.
- **Список сжат до столбика аватарок** (открыт форум) — плиток нет: там
  нет и текста.
- Строки со значком вместо аватарки — «Избранное», «Ответы», удалённые —
  получают плитку цвета темы.

**Экономия:** аватарка размывается один раз, уменьшенной до 32 точек, —
три прохода коробчатого размытия, почти как гауссово. Готовые хранятся в
памяти, до 256 переписок: при прокрутке список показывает одни и те же
снова и снова. Выключены плитки — ничего не грузится и не размывается.

**Как устроено:** в самой строке Telegram — одно новое поле и три вызова,
11 строк. Всё остальное — в своём файле рядом.

**Заодно проверка 1.4.1:** эта сборка не должна трогать `TelegramCore`.
Смотрим на время.

---

## Версия 1.7 · 10 сентября — в испытании

Заплатка `0008`, **частицы как на ПК**. В 1.1 была одна мягкая белая
точка — теперь те же пять видов, что на ПК, и нарисованы так же:

| Вид | Как нарисован |
|---|---|
| Снежинки | шесть лучей с отростками |
| Сакура | лепесток с выемкой на кончике, розовый к низу |
| Матрица | струйка: яркая голова внизу, гаснущий хвост |
| Дождь | короткая черта, светлеющая книзу |
| Осенние листья | три цвета, с прожилкой |

- В разделе «Lunagram» — вид, густота (редко, средне, густо) и скорость
  (медленно, обычно, быстро). Выбрал вид — частицы включаются сами.
- Матрица и дождь падают быстро и прямо, без вращения; остальные медленно,
  с кружением и сносом в стороны — как на ПК.

**Как перенесено.** На ПК каждая частица двигается нашим кодом на каждом
кадре. Здесь её по-прежнему ведёт сама система (`CAEmitterLayer`), на
видеокарте, — а числа взяты с ПК: скорость в долях высоты экрана,
разброс размера и прозрачности, вращение. На высоком экране частицы
летят быстрее, так что время полёта сверху донизу одинаковое везде.

**Густота — это сколько частиц на экране разом**, как на ПК: «средне» —
сотня, как там по умолчанию. Рождаются они так часто, чтобы за время
полёта набралось столько, — при смене скорости это пересчитывается.

**Заодно:** картинки частиц рисуются кодом с тройной чёткостью — резкие на
любом экране, а в сборке по-прежнему ни одной картинки.
