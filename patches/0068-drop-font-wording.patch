diff --git a/build-system/GenerateStrings/GenerateStrings.py b/build-system/GenerateStrings/GenerateStrings.py
index 3a1539c5..e10ff2b8 100644
--- a/build-system/GenerateStrings/GenerateStrings.py
+++ b/build-system/GenerateStrings/GenerateStrings.py
@@ -445,255 +445,6 @@ static NSString * _Nonnull getPluralizationSuffix(uint32_t lc, int32_t value) {
     }
 }
 
-
-// Lunagram: свои надписи вместо телеграмовских — «языки», как на картинке у
-// хозяина. Меняется только то, что человек видит; ключи строк и всё, что на
-// них завязано, остаются телеграмовскими.
-//
-// Крючок стоит в самом низу, в getSingle: через неё проходит каждая надпись
-// клиента, поэтому один набор накрывает весь интерфейс разом.
-static NSDictionary<NSString *, NSString *> * _Nullable lunagramWordingDict(NSInteger pack) {
-    static NSLock *lock = nil;
-    static NSInteger cachedPack = 0;
-    static NSDictionary<NSString *, NSString *> *cachedDict = nil;
-    static dispatch_once_t onceToken;
-    dispatch_once(&onceToken, ^{
-        lock = [[NSLock alloc] init];
-    });
-
-    [lock lock];
-    if (cachedPack == pack && cachedDict != nil) {
-        NSDictionary<NSString *, NSString *> *result = cachedDict;
-        [lock unlock];
-        return result;
-    }
-
-    NSDictionary<NSString *, NSString *> *dict = nil;
-    switch (pack) {
-        case 1: {
-            // Доксграм
-            dict = @{
-                @"Conversation.ContextMenuReply": @"Отписать",
-                @"Conversation.ContextMenuCopy": @"Спереть",
-                @"Conversation.ContextMenuForward": @"Вжух",
-                @"Conversation.ContextMenuDelete": @"Снести",
-                @"Conversation.ContextMenuSelect": @"Отметить",
-                @"Conversation.ContextMenuShare": @"Раскидать",
-                @"Conversation.ContextMenuMore": @"Дальше...",
-                @"Conversation.ContextMenuCopyLink": @"Спереть ссылку",
-                @"Conversation.ContextMenuBan": @"Урезать",
-                @"Conversation.Pin": @"Прибить",
-                @"Conversation.Unpin": @"Отодрать",
-                @"Conversation.Edit": @"Переписать",
-                @"Conversation.Info": @"Досье",
-                @"Conversation.Search": @"Пробить",
-                @"Conversation.Mute": @"Заткнуть",
-                @"Conversation.Unmute": @"Развязать язык",
-                @"Conversation.Block": @"Забанить",
-                @"Conversation.Unblock": @"Разбанить",
-                @"Conversation.Report": @"Настучать",
-                @"ChatList.Mute": @"Заткнуть",
-                @"ChatList.Unmute": @"Развязать язык",
-                @"ChatList.ReadAll": @"Прочесть всё",
-                @"ChatList.ArchiveAction": @"Чулан",
-                @"Common.OK": @"Норм",
-                @"Common.Cancel": @"Забей",
-                @"Common.Done": @"Всё",
-                @"Common.Delete": @"Снести",
-                @"Common.Back": @"Обратно",
-                @"Common.Next": @"Дальше",
-                @"Common.Close": @"Свалить",
-                @"Common.Yes": @"Ага",
-                @"Common.No": @"Не",
-                @"Common.Save": @"Заныкать",
-                @"Common.Edit": @"Переписать",
-                @"Common.Search": @"Пробить",
-                @"Common.More": @"Больше",
-                @"Common.Select": @"Отметить",
-                @"Settings.Title": @"Крутилки",
-                @"Settings.Appearance": @"Внешка",
-                @"Settings.Logout": @"Слиться",
-                @"Settings.SavedMessages": @"Заначка",
-                @"Settings.Search": @"Пробить настройки",
-                @"Contacts.Title": @"Кореша",
-                @"Calls.TabTitle": @"Звонилка",
-            };
-            break;
-        }
-        case 2: {
-            // Ангельский
-            dict = @{
-                @"Conversation.ContextMenuReply": @"Откликнуться",
-                @"Conversation.ContextMenuCopy": @"Переписать себе",
-                @"Conversation.ContextMenuForward": @"Передать",
-                @"Conversation.ContextMenuDelete": @"Отпустить",
-                @"Conversation.ContextMenuSelect": @"Отметить",
-                @"Conversation.ContextMenuShare": @"Подарить",
-                @"Conversation.ContextMenuMore": @"Ещё...",
-                @"Conversation.ContextMenuCopyLink": @"Взять ссылку",
-                @"Conversation.ContextMenuBan": @"Ограничить",
-                @"Conversation.Pin": @"Сберечь",
-                @"Conversation.Unpin": @"Снять",
-                @"Conversation.Edit": @"Поправить",
-                @"Conversation.Info": @"О нём",
-                @"Conversation.Search": @"Найти",
-                @"Conversation.Mute": @"Утишить",
-                @"Conversation.Unmute": @"Вернуть голос",
-                @"Conversation.Block": @"Затворить",
-                @"Conversation.Unblock": @"Отворить",
-                @"Conversation.Report": @"Просить помощи",
-                @"ChatList.Mute": @"Утишить",
-                @"ChatList.Unmute": @"Вернуть голос",
-                @"ChatList.ReadAll": @"Принять всё",
-                @"ChatList.ArchiveAction": @"Затвор",
-                @"Common.OK": @"Хорошо",
-                @"Common.Cancel": @"Не надо",
-                @"Common.Done": @"Готово",
-                @"Common.Delete": @"Отпустить",
-                @"Common.Back": @"Назад",
-                @"Common.Next": @"Дальше",
-                @"Common.Close": @"Закрыть",
-                @"Common.Yes": @"Да",
-                @"Common.No": @"Нет",
-                @"Common.Save": @"Сберечь",
-                @"Common.Edit": @"Поправить",
-                @"Common.Search": @"Найти",
-                @"Common.More": @"Ещё",
-                @"Common.Select": @"Отметить",
-                @"Settings.Title": @"Уклад",
-                @"Settings.Appearance": @"Облик",
-                @"Settings.Logout": @"Проститься",
-                @"Settings.SavedMessages": @"Тихая гавань",
-                @"Settings.Search": @"Найти в укладе",
-                @"Contacts.Title": @"Близкие",
-                @"Calls.TabTitle": @"Голоса",
-            };
-            break;
-        }
-        case 3: {
-            // Майнкрафт
-            dict = @{
-                @"Conversation.ContextMenuReply": @"Скрафтить ответ",
-                @"Conversation.ContextMenuCopy": @"Дюпнуть",
-                @"Conversation.ContextMenuForward": @"Телепортнуть",
-                @"Conversation.ContextMenuDelete": @"Сломать",
-                @"Conversation.ContextMenuSelect": @"Выделить",
-                @"Conversation.ContextMenuShare": @"Дропнуть",
-                @"Conversation.ContextMenuMore": @"Ещё...",
-                @"Conversation.ContextMenuCopyLink": @"Скопировать координаты",
-                @"Conversation.ContextMenuBan": @"Забрать права",
-                @"Conversation.Pin": @"Поставить блок",
-                @"Conversation.Unpin": @"Сломать блок",
-                @"Conversation.Edit": @"Зачаровать",
-                @"Conversation.Info": @"Инфо",
-                @"Conversation.Search": @"Найти",
-                @"Conversation.Mute": @"Замьютить",
-                @"Conversation.Unmute": @"Размьютить",
-                @"Conversation.Block": @"Забанить",
-                @"Conversation.Unblock": @"Разбанить",
-                @"Conversation.Report": @"Позвать админа",
-                @"ChatList.Mute": @"Замьютить",
-                @"ChatList.Unmute": @"Размьютить",
-                @"ChatList.ReadAll": @"Прочитать всё",
-                @"ChatList.ArchiveAction": @"Сундук",
-                @"Common.OK": @"Принять",
-                @"Common.Cancel": @"Отмена",
-                @"Common.Done": @"Скрафчено",
-                @"Common.Delete": @"Сломать",
-                @"Common.Back": @"Назад",
-                @"Common.Next": @"Дальше",
-                @"Common.Close": @"Закрыть сундук",
-                @"Common.Yes": @"Ага",
-                @"Common.No": @"Неа",
-                @"Common.Save": @"Сохранить мир",
-                @"Common.Edit": @"Зачаровать",
-                @"Common.Search": @"Найти",
-                @"Common.More": @"Ещё",
-                @"Common.Select": @"Выделить",
-                @"Settings.Title": @"Настройки мира",
-                @"Settings.Appearance": @"Ресурспак",
-                @"Settings.Logout": @"Выйти из игры",
-                @"Settings.SavedMessages": @"Эндер-сундук",
-                @"Settings.Search": @"Найти настройку",
-                @"Contacts.Title": @"Игроки",
-                @"Calls.TabTitle": @"Голосовой чат",
-            };
-            break;
-        }
-        case 4: {
-            // Готический
-            dict = @{
-                @"Conversation.ContextMenuReply": @"Изречь",
-                @"Conversation.ContextMenuCopy": @"Списать",
-                @"Conversation.ContextMenuForward": @"Разослать",
-                @"Conversation.ContextMenuDelete": @"Предать забвению",
-                @"Conversation.ContextMenuSelect": @"Избрать",
-                @"Conversation.ContextMenuShare": @"Возвестить",
-                @"Conversation.ContextMenuMore": @"Далее...",
-                @"Conversation.ContextMenuCopyLink": @"Списать ссылку",
-                @"Conversation.ContextMenuBan": @"Умалить",
-                @"Conversation.Pin": @"Пригвоздить",
-                @"Conversation.Unpin": @"Снять",
-                @"Conversation.Edit": @"Переиначить",
-                @"Conversation.Info": @"Сведения",
-                @"Conversation.Search": @"Взыскать",
-                @"Conversation.Mute": @"Обречь на немоту",
-                @"Conversation.Unmute": @"Вернуть глас",
-                @"Conversation.Block": @"Изгнать",
-                @"Conversation.Unblock": @"Простить",
-                @"Conversation.Report": @"Донести",
-                @"ChatList.Mute": @"Обречь на немоту",
-                @"ChatList.Unmute": @"Вернуть глас",
-                @"ChatList.ReadAll": @"Прочесть всё",
-                @"ChatList.ArchiveAction": @"Склеп",
-                @"Common.OK": @"Быть по сему",
-                @"Common.Cancel": @"Отречься",
-                @"Common.Done": @"Свершилось",
-                @"Common.Delete": @"Предать забвению",
-                @"Common.Back": @"Вспять",
-                @"Common.Next": @"Далее",
-                @"Common.Close": @"Затворить",
-                @"Common.Yes": @"Истинно",
-                @"Common.No": @"Отнюдь",
-                @"Common.Save": @"Сберечь",
-                @"Common.Edit": @"Переиначить",
-                @"Common.Search": @"Взыскать",
-                @"Common.More": @"Более",
-                @"Common.Select": @"Избрать",
-                @"Settings.Title": @"Уложение",
-                @"Settings.Appearance": @"Убранство",
-                @"Settings.Logout": @"Отринуть",
-                @"Settings.SavedMessages": @"Скрижали",
-                @"Settings.Search": @"Взыскать в уложении",
-                @"Contacts.Title": @"Ближние",
-                @"Calls.TabTitle": @"Зовы",
-            };
-            break;
-        }
-        default: {
-            break;
-        }
-    }
-
-    cachedPack = pack;
-    cachedDict = dict;
-    [lock unlock];
-    return dict;
-}
-
-static NSString * _Nonnull lunagramApplyWording(NSString * _Nonnull key, NSString * _Nonnull value) {
-    NSInteger pack = [[NSUserDefaults standardUserDefaults] integerForKey:@"lunagram.wording.pack"];
-    if (pack <= 0) {
-        return value;
-    }
-    NSDictionary<NSString *, NSString *> *dict = lunagramWordingDict(pack);
-    NSString *replacement = dict[key];
-    if (replacement != nil) {
-        return replacement;
-    }
-    return value;
-}
-
 static NSString * _Nonnull getSingle(_PresentationStrings * _Nullable strings, NSString * _Nonnull key,
     bool * _Nullable isFound) {
     NSString *result = nil;
@@ -733,7 +484,7 @@ static NSString * _Nonnull getSingle(_PresentationStrings * _Nullable strings, N
             *isFound = true;
         }
     }
-    return lunagramApplyWording(key, result);
+    return result;
 }
 
 static NSString * _Nonnull getSingleIndirect(_PresentationStrings * _Nonnull strings, uint32_t keyId) {
diff --git a/submodules/Display/BUILD b/submodules/Display/BUILD
index 358835c0..8e56c1d1 100644
--- a/submodules/Display/BUILD
+++ b/submodules/Display/BUILD
@@ -16,7 +16,6 @@ swift_library(
     	"//submodules/SSignalKit/SwiftSignalKit:SwiftSignalKit",
         "//submodules/Markdown:Markdown",
         "//submodules/AsyncDisplayKit:AsyncDisplayKit",
-        "//submodules/LunagramFont",
     ],
     visibility = [
         "//visibility:public",
diff --git a/submodules/Display/Source/Font.swift b/submodules/Display/Source/Font.swift
index 8856c76a..4f1a4c0c 100644
--- a/submodules/Display/Source/Font.swift
+++ b/submodules/Display/Source/Font.swift
@@ -1,6 +1,5 @@
 import Foundation
 import UIKit
-import LunagramFont
 
 public struct Font {
     public enum Design {
@@ -159,25 +158,9 @@ public struct Font {
 
     private static let cache = Cache()
     
-    /// Lunagram: свой шрифт вместо системного, если он выбран.
-    ///
-    /// Моноширинный и «камеру» не трогаем: в первом лежит код и ссылки, где
-    /// важна ровная ширина, второй — служебный шрифт съёмки.
-    private static func lunagramFont(size: CGFloat, design: Design, weight: Weight, traits: Traits) -> UIFont? {
-        switch design {
-        case .monospace, .camera:
-            return nil
-        default:
-            return LunagramFontSettings.font(size: size, weight: weight.weight, italic: traits.contains(.italic))
-        }
-    }
-
     public static func with(size: CGFloat, design: Design = .regular, weight: Weight = .regular, width: Width = .standard, traits: Traits = []) -> UIFont {
-        if let font = lunagramFont(size: size, design: design, weight: weight, traits: traits) {
-            return font
-        }
         let key = "\(size)_\(design.key)_\(weight.key)_\(width.key)_\(traits.rawValue)"
-
+        
         if let cachedFont = self.cache.get(key) {
             return cachedFont
         }
@@ -296,30 +279,18 @@ public struct Font {
     }
     
     public static func regular(_ size: CGFloat) -> UIFont {
-        if let font = LunagramFontSettings.font(size: size, weight: .regular, italic: false) {
-            return font
-        }
         return UIFont.systemFont(ofSize: size)
     }
-
+    
     public static func medium(_ size: CGFloat) -> UIFont {
-        if let font = LunagramFontSettings.font(size: size, weight: .medium, italic: false) {
-            return font
-        }
         return UIFont.systemFont(ofSize: size, weight: UIFont.Weight.medium)
     }
-
+    
     public static func semibold(_ size: CGFloat) -> UIFont {
-        if let font = LunagramFontSettings.font(size: size, weight: .semibold, italic: false) {
-            return font
-        }
         return UIFont.systemFont(ofSize: size, weight: UIFont.Weight.semibold)
     }
-
+    
     public static func bold(_ size: CGFloat) -> UIFont {
-        if let font = LunagramFontSettings.font(size: size, weight: .bold, italic: false) {
-            return font
-        }
         if #available(iOS 8.2, *) {
             return UIFont.boldSystemFont(ofSize: size)
         } else {
@@ -332,16 +303,10 @@ public struct Font {
     }
     
     public static func light(_ size: CGFloat) -> UIFont {
-        if let font = LunagramFontSettings.font(size: size, weight: .light, italic: false) {
-            return font
-        }
         return UIFont.systemFont(ofSize: size, weight: UIFont.Weight.light)
     }
-
+    
     public static func semiboldItalic(_ size: CGFloat) -> UIFont {
-        if let font = LunagramFontSettings.font(size: size, weight: .semibold, italic: true) {
-            return font
-        }
         if let descriptor = UIFont.systemFont(ofSize: size).fontDescriptor.withSymbolicTraits([.traitBold, .traitItalic]) {
             return UIFont(descriptor: descriptor, size: size)
         } else {
@@ -366,9 +331,6 @@ public struct Font {
     }
     
     public static func italic(_ size: CGFloat) -> UIFont {
-        if let font = LunagramFontSettings.font(size: size, weight: .regular, italic: true) {
-            return font
-        }
         return UIFont.italicSystemFont(ofSize: size)
     }
 }
diff --git a/submodules/Lunagram/BUILD b/submodules/Lunagram/BUILD
index 88e53eeb..6cacc4a0 100644
--- a/submodules/Lunagram/BUILD
+++ b/submodules/Lunagram/BUILD
@@ -5,8 +5,8 @@ load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")
 # могла подключить любая часть приложения, даже самая низкоуровневая,
 # вроде фона переписки.
 #
-# Кроме Ghost и шрифта: их читают TelegramCore и Display, поэтому они живут
-# в LunagramGhost и LunagramFont. Подробности — в BUILD тех модулей.
+# Кроме Ghost: его читает TelegramCore, поэтому он живёт в LunagramGhost.
+# Подробности — в BUILD того модуля.
 swift_library(
     name = "Lunagram",
     module_name = "Lunagram",
@@ -18,7 +18,6 @@ swift_library(
     ],
     deps = [
         "//submodules/LunagramGhost",
-        "//submodules/LunagramFont",
     ],
     visibility = [
         "//visibility:public",
diff --git a/submodules/Lunagram/Sources/LunagramSettings.swift b/submodules/Lunagram/Sources/LunagramSettings.swift
index 73a20aaf..353577ea 100644
--- a/submodules/Lunagram/Sources/LunagramSettings.swift
+++ b/submodules/Lunagram/Sources/LunagramSettings.swift
@@ -2,7 +2,6 @@ import Foundation
 import CoreGraphics
 import ImageIO
 import UIKit
-import LunagramFont
 import LunagramGhost
 
 /// Настройки Lunagram.
@@ -589,63 +588,6 @@ public final class LunagramSettings {
         }
     }
 
-    // MARK: Свой шрифт интерфейса
-    //
-    // Само значение — в LunagramFont, его читает Display. Здесь переходники,
-    // чтобы экран настроек ходил в одно место.
-
-    /// Какое начертание выбрано: 0 — системное, дальше готовые, и отдельно
-    /// свой файл.
-    public static var fontStyle: Int {
-        get { return LunagramFontSettings.style }
-        set {
-            LunagramFontSettings.style = newValue
-            changed()
-        }
-    }
-
-    /// Готовые начертания, взятые из самого айфона.
-    public static var fontStyles: [(id: Int, title: String)] {
-        return LunagramFontSettings.builtInStyles
-    }
-
-    /// Номер, которым обозначен свой файл шрифта.
-    public static var ownFontStyle: Int {
-        return LunagramFontSettings.ownFileStyle
-    }
-
-    /// Есть ли в выбранном файле русские буквы.
-    public static var customFontHasCyrillic: Bool {
-        return LunagramFontSettings.fileHasCyrillic
-    }
-
-    /// Шрифт для показа образца в списке начертаний: строка пишется тем
-    /// шрифтом, который она выбирает, — иначе все выглядят одинаково.
-    public static func sampleFont(styleIndex: Int, size: CGFloat) -> UIFont? {
-        return LunagramFontSettings.sampleFont(styleIndex: styleIndex, size: size)
-    }
-
-    /// Название выбранного шрифта, или `nil`, если файла нет.
-    public static var customFontTitle: String? {
-        return LunagramFontSettings.title
-    }
-
-    public static var hasCustomFont: Bool {
-        return LunagramFontSettings.hasFont
-    }
-
-    /// Забирает файл шрифта себе.
-    public static func importFont(from source: URL) throws {
-        try LunagramFontSettings.importFont(from: source)
-        changed()
-    }
-
-    /// Убирает свой шрифт — вернётся системный.
-    public static func clearCustomFont() {
-        LunagramFontSettings.clearFont()
-        changed()
-    }
-
     // MARK: Вид имени в профиле
 
     /// Цвета имени в шапке профиля — как на ПК. Ноль оставлен под обычный
@@ -887,25 +829,6 @@ public final class LunagramSettings {
         return UIImage.animatedImage(with: frames, duration: duration)
     }
 
-    // MARK: Надписи интерфейса
-    //
-    // Ключ читает не Swift, а сам генератор строк Telegram — тот кусок, через
-    // который проходит каждая надпись клиента. Поэтому имя ключа менять
-    // нельзя, не поправив заплатку генератора.
-
-    /// Набор надписей: 0 — родные телеграмовские, дальше по порядку
-    /// Доксграм, Ангельский, Майнкрафт, Готический.
-    public static var wordingPack: Int {
-        get { return defaults.integer(forKey: "lunagram.wording.pack") }
-        set {
-            defaults.set(newValue, forKey: "lunagram.wording.pack")
-            changed()
-        }
-    }
-
-    /// Названия наборов — по порядку, начиная с обычного.
-    public static let wordingPackTitles = ["Обычные", "Доксграм", "Ангельский", "Майнкрафт", "Готический"]
-
     // MARK: Живые обои
 
     /// Видео вместо обоев переписки.
diff --git a/submodules/LunagramFont/BUILD b/submodules/LunagramFont/BUILD
deleted file mode 100644
index 97d98a8f..00000000
--- a/submodules/LunagramFont/BUILD
+++ /dev/null
@@ -1,21 +0,0 @@
-load("@build_bazel_rules_swift//swift:swift.bzl", "swift_library")
-
-# Свой шрифт интерфейса — отдельно от остального Lunagram.
-#
-# Его читает Display, самый низ приложения: через него идёт весь текст.
-# Всё, от чего Display зависит, при правке пересобирает приложение целиком.
-# Поэтому здесь только шрифт, и меняется это редко. Та же причина, по
-# которой Ghost живёт в LunagramGhost.
-swift_library(
-    name = "LunagramFont",
-    module_name = "LunagramFont",
-    srcs = glob([
-        "Sources/**/*.swift",
-    ]),
-    copts = [
-        "-warnings-as-errors",
-    ],
-    visibility = [
-        "//visibility:public",
-    ],
-)
diff --git a/submodules/LunagramFont/Sources/LunagramFontSettings.swift b/submodules/LunagramFont/Sources/LunagramFontSettings.swift
deleted file mode 100644
index 67dda136..00000000
--- a/submodules/LunagramFont/Sources/LunagramFontSettings.swift
+++ /dev/null
@@ -1,311 +0,0 @@
-import CoreText
-import Foundation
-import UIKit
-
-/// Свой шрифт на весь интерфейс.
-///
-/// Живёт отдельным крошечным модулем по той же причине, что и Ghost: его
-/// читает `Display` — самый низ приложения, через него идёт вообще весь
-/// текст. Если бы шрифт лежал в общем модуле Lunagram, каждая мелкая
-/// правка любой нашей функции пересобирала бы всё приложение. Здесь только
-/// то, что нужно `Display`, и меняется это редко.
-public enum LunagramFontSettings {
-    private static let defaults = UserDefaults.standard
-
-    private enum Key {
-        static let style = "lunagram.font.style"
-        static let file = "lunagram.font.file"
-        static let title = "lunagram.font.title"
-        static let fileHasCyrillic = "lunagram.font.fileCyrillic"
-    }
-
-    /// Сообщается, когда шрифт сменили: экраны по ней перерисовываются.
-    public static let didChangeNotification = Notification.Name("LunagramFontSettings.didChange")
-
-    // Всё ниже — под замком: `Font.regular(...)` зовут и с главной очереди,
-    // и с фоновых, где Telegram считает разметку.
-    private static let lock = NSLock()
-    private static var cache: [String: UIFont] = [:]
-    private static var registeredFile: String?
-    private static var familyName: String?
-    /// Выбранное начертание держим в памяти: его спрашивают на каждую
-    /// строчку текста, а не раз в экран.
-    private static var styleCache: Int?
-
-    /// Как называется выбранный шрифт — показываем в настройках.
-    public static var title: String? {
-        return defaults.string(forKey: Key.title)
-    }
-
-    /// Есть ли вообще выбранный файл шрифта.
-    public static var hasFont: Bool {
-        return defaults.string(forKey: Key.file) != nil
-    }
-
-    /// Забирает файл шрифта себе. Имя семейства читаем из самого файла:
-    /// у шрифта оно своё и с именем файла обычно не совпадает.
-    public static func importFont(from source: URL) throws {
-        let folder = try lunagramFolder()
-        let ext = source.pathExtension.isEmpty ? "ttf" : source.pathExtension
-        let name = "font-\(UUID().uuidString).\(ext)"
-        let target = folder.appendingPathComponent(name)
-        // Файл из «Файлов» лежит в песочнице чужого приложения — читаем
-        // содержимое, а не переносим ссылку.
-        let data = try Data(contentsOf: source)
-        try data.write(to: target)
-
-        guard let family = readFamilyName(of: target) else {
-            try? FileManager.default.removeItem(at: target)
-            throw ImportError.notAFont
-        }
-
-        clearStoredFile()
-        defaults.set(name, forKey: Key.file)
-        defaults.set(family, forKey: Key.title)
-        // Сразу смотрим, есть ли в файле русские буквы: если нет, честно
-        // скажем об этом в настройках, а не оставим человека с квадратами.
-        CTFontManagerRegisterFontsForURL(target as CFURL, .process, nil)
-        let cyrillic = UIFont(name: family, size: 12.0).map { hasCyrillic($0) } ?? false
-        defaults.set(cyrillic, forKey: Key.fileHasCyrillic)
-        defaults.set(ownFileStyle, forKey: Key.style)
-        invalidate()
-    }
-
-    /// Есть ли в выбранном файле русские буквы.
-    public static var fileHasCyrillic: Bool {
-        return defaults.bool(forKey: Key.fileHasCyrillic)
-    }
-
-    /// Убирает свой файл шрифта — вернётся системный.
-    public static func clearFont() {
-        clearStoredFile()
-        defaults.removeObject(forKey: Key.title)
-        defaults.removeObject(forKey: Key.fileHasCyrillic)
-        if style == ownFileStyle {
-            defaults.set(0, forKey: Key.style)
-        }
-        invalidate()
-    }
-
-    /// Шрифт под запрошенный размер и насыщенность, или `nil` — тогда
-    /// `Display` возьмёт родной системный.
-    ///
-    /// Насыщенность и наклон подмешиваем поверх: у взятого со стороны
-    /// шрифта обычно одно начертание, а Telegram просит и жирное, и
-    /// курсив. Где их нет, останется обычное — это лучше, чем потерять
-    /// свой шрифт на половине надписей.
-    public static func font(size: CGFloat, weight: UIFont.Weight, italic: Bool) -> UIFont? {
-        let style = self.style
-        guard style != 0 else {
-            return nil
-        }
-        let key = "\(style)_\(size)_\(weight.rawValue)_\(italic ? 1 : 0)"
-
-        lock.lock()
-        if let cached = cache[key] {
-            lock.unlock()
-            return cached
-        }
-        var base: UIFont?
-        if style == LunagramFontSettings.ownFileStyle {
-            if let family = self.resolvedFamily() {
-                base = UIFont(name: family, size: size)
-            }
-        } else {
-            base = builtInFont(style: style, size: size)
-        }
-        lock.unlock()
-
-        guard let base = base else {
-            return nil
-        }
-
-        var traits: UIFontDescriptor.SymbolicTraits = []
-        // UIFont.Weight сравнивать напрямую нельзя — сравниваем числа.
-        if weight.rawValue >= UIFont.Weight.semibold.rawValue {
-            traits.insert(.traitBold)
-        }
-        if italic {
-            traits.insert(.traitItalic)
-        }
-        var font = base
-        if !traits.isEmpty, let descriptor = base.fontDescriptor.withSymbolicTraits(traits) {
-            font = UIFont(descriptor: descriptor, size: size)
-        }
-
-        lock.lock()
-        cache[key] = font
-        lock.unlock()
-        return font
-    }
-
-    // MARK: Готовые начертания
-
-    /// Номер, которым обозначен «свой файл» — чтобы не путать его со
-    /// встроенными.
-    public static let ownFileStyle = 100
-
-    /// Готовые начертания: берутся из самого айфона, качать ничего не надо.
-    /// Каждое проверено на русские буквы — те, где их нет, сюда не попали.
-    ///
-    /// Первые два — сам системный шрифт, только другой выделки: у Apple к
-    /// нему идут скруглённый и с засечками варианты, и кириллица в них
-    /// родная. Остальные — обычные шрифты, лежащие в системе с давних пор.
-    public static let builtInStyles: [(id: Int, title: String)] = [
-        (1, "Округлый"),
-        (2, "С засечками"),
-        (3, "Печатная машинка"),
-        (4, "Книжный"),
-        (5, "Плакатный"),
-        (6, "Широкий"),
-        (7, "Узкий")
-    ]
-
-    /// Какое начертание выбрано: 0 — системное, 1…7 — готовое,
-    /// `ownFileStyle` — свой файл.
-    public static var style: Int {
-        get {
-            lock.lock()
-            defer { lock.unlock() }
-            if let styleCache = self.styleCache {
-                return styleCache
-            }
-            let value = defaults.integer(forKey: Key.style)
-            self.styleCache = value
-            return value
-        }
-        set {
-            defaults.set(newValue, forKey: Key.style)
-            invalidate()
-        }
-    }
-
-    /// Шрифт готового начертания. Если в нём вдруг не окажется русских
-    /// букв — не берём его вовсе: лучше системный, чем пустые квадраты.
-    private static func builtInFont(style: Int, size: CGFloat) -> UIFont? {
-        switch style {
-        case 1, 2:
-            // Системный шрифт другой выделки — кириллица в нём родная.
-            let design: UIFontDescriptor.SystemDesign = style == 1 ? .rounded : .serif
-            guard let descriptor = UIFont.systemFont(ofSize: size).fontDescriptor.withDesign(design) else {
-                return nil
-            }
-            return UIFont(descriptor: descriptor, size: size)
-        default:
-            // Имена пишем по-разному: у одних шрифтов система знает
-            // семейство, у других — только внутреннее имя начертания.
-            let candidates: [String]
-            switch style {
-            case 3:
-                candidates = ["CourierNewPSMT", "Courier New", "Courier"]
-            case 4:
-                candidates = ["Georgia", "TimesNewRomanPSMT"]
-            case 5:
-                candidates = ["Arial-Black", "Arial Black", "HelveticaNeue-Bold"]
-            case 6:
-                candidates = ["Verdana", "Tahoma"]
-            case 7:
-                candidates = ["AvenirNextCondensed-Regular", "Avenir Next Condensed", "HelveticaNeue-CondensedBold"]
-            default:
-                return nil
-            }
-            for name in candidates {
-                if let font = UIFont(name: name, size: size), hasCyrillic(font) {
-                    return font
-                }
-            }
-            return nil
-        }
-    }
-
-    /// Шрифт для показа образца в списке: 0 — системный, 1…7 — готовые,
-    /// `ownFileStyle` — свой файл. Нужен, чтобы строка выбора писалась тем
-    /// самым шрифтом и было видно, какой из них какой.
-    public static func sampleFont(styleIndex: Int, size: CGFloat) -> UIFont? {
-        if styleIndex == 0 {
-            return nil
-        }
-        if styleIndex == ownFileStyle {
-            lock.lock()
-            let family = resolvedFamily()
-            lock.unlock()
-            guard let family = family else {
-                return nil
-            }
-            return UIFont(name: family, size: size)
-        }
-        return builtInFont(style: styleIndex, size: size)
-    }
-
-    /// Есть ли в шрифте русские буквы. Проверяем по-настоящему — просим у
-    /// него очертания для «А» и «я»; если хоть одного нет, шрифт не годится.
-    public static func hasCyrillic(_ font: UIFont) -> Bool {
-        let characters: [UniChar] = Array("Ая".utf16)
-        var glyphs = [CGGlyph](repeating: 0, count: characters.count)
-        let found = CTFontGetGlyphsForCharacters(font as CTFont, characters, &glyphs, characters.count)
-        return found && !glyphs.contains(0)
-    }
-
-    // MARK: Внутреннее
-
-    private enum ImportError: Error {
-        case notAFont
-    }
-
-    /// Подключает файл шрифта к системе, если ещё не подключён, и
-    /// возвращает имя семейства. Звать только под замком.
-    private static func resolvedFamily() -> String? {
-        guard let fileName = defaults.string(forKey: Key.file) else {
-            return nil
-        }
-        if fileName == registeredFile, let familyName = self.familyName {
-            return familyName
-        }
-        guard let folder = try? lunagramFolder() else {
-            return nil
-        }
-        let url = folder.appendingPathComponent(fileName)
-        // Подключаем в своё приложение; если уже подключён — это не ошибка.
-        CTFontManagerRegisterFontsForURL(url as CFURL, .process, nil)
-        let family = readFamilyName(of: url)
-        registeredFile = fileName
-        self.familyName = family
-        return family
-    }
-
-    /// Имя, по которому шрифт потом можно попросить у системы.
-    private static func readFamilyName(of url: URL) -> String? {
-        guard let descriptors = CTFontManagerCreateFontDescriptorsFromURL(url as CFURL) as? [CTFontDescriptor], let first = descriptors.first else {
-            return nil
-        }
-        let font = CTFontCreateWithFontDescriptor(first, 0.0, nil)
-        return CTFontCopyPostScriptName(font) as String
-    }
-
-    private static func clearStoredFile() {
-        if let folder = try? lunagramFolder(), let name = defaults.string(forKey: Key.file) {
-            try? FileManager.default.removeItem(at: folder.appendingPathComponent(name))
-        }
-        defaults.removeObject(forKey: Key.file)
-    }
-
-    private static func invalidate() {
-        lock.lock()
-        cache.removeAll()
-        registeredFile = nil
-        familyName = nil
-        styleCache = nil
-        lock.unlock()
-        NotificationCenter.default.post(name: didChangeNotification, object: nil)
-    }
-
-    /// Та же папка, что у остального Lunagram. Повторена здесь нарочно:
-    /// этот модуль не должен зависеть от общего, иначе вернётся долгая
-    /// пересборка, ради ухода от которой он и заведён.
-    private static func lunagramFolder() throws -> URL {
-        let base = try FileManager.default.url(for: .applicationSupportDirectory, in: .userDomainMask, appropriateFor: nil, create: true)
-        let folder = base.appendingPathComponent("Lunagram", isDirectory: true)
-        try FileManager.default.createDirectory(at: folder, withIntermediateDirectories: true, attributes: nil)
-        return folder
-    }
-}
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/LunagramFontChoiceItem.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/LunagramFontChoiceItem.swift
deleted file mode 100644
index cb254e81..00000000
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/LunagramFontChoiceItem.swift
+++ /dev/null
@@ -1,186 +0,0 @@
-import Foundation
-import UIKit
-import Display
-import AsyncDisplayKit
-import SwiftSignalKit
-import TelegramPresentationData
-import ItemListUI
-import PresentationDataUtils
-
-/// Строка выбора шрифта: название написано ТЕМ САМЫМ шрифтом, чтобы было
-/// видно, какой из них какой. Обычная строка-галочка этого не умеет — у неё
-/// шрифт всегда системный.
-final class LunagramFontChoiceItem: ListViewItem, ItemListItem {
-    let presentationData: ItemListPresentationData
-    let title: String
-    /// Каким шрифтом рисовать название. Пусто — системным.
-    let sampleFont: UIFont?
-    let isSelected: Bool
-    let sectionId: ItemListSectionId
-    let action: () -> Void
-
-    init(presentationData: ItemListPresentationData, title: String, sampleFont: UIFont?, isSelected: Bool, sectionId: ItemListSectionId, action: @escaping () -> Void) {
-        self.presentationData = presentationData
-        self.title = title
-        self.sampleFont = sampleFont
-        self.isSelected = isSelected
-        self.sectionId = sectionId
-        self.action = action
-    }
-
-    var selectable: Bool = true
-
-    func selected(listView: ListView) {
-        listView.clearHighlightAnimated(true)
-        self.action()
-    }
-
-    func nodeConfiguredForParams(async: @escaping (@escaping () -> Void) -> Void, params: ListViewItemLayoutParams, synchronousLoads: Bool, previousItem: ListViewItem?, nextItem: ListViewItem?, completion: @escaping (ListViewItemNode, @escaping () -> (Signal<Void, NoError>?, (ListViewItemApply) -> Void)) -> Void) {
-        async {
-            let node = LunagramFontChoiceItemNode()
-            let (layout, apply) = node.asyncLayout()(self, params, itemListNeighbors(item: self, topItem: previousItem as? ItemListItem, bottomItem: nextItem as? ItemListItem))
-            node.contentSize = layout.contentSize
-            node.insets = layout.insets
-            Queue.mainQueue().async {
-                completion(node, {
-                    return (nil, { _ in apply() })
-                })
-            }
-        }
-    }
-
-    func updateNode(async: @escaping (@escaping () -> Void) -> Void, node: @escaping () -> ListViewItemNode, params: ListViewItemLayoutParams, previousItem: ListViewItem?, nextItem: ListViewItem?, animation: ListViewItemUpdateAnimation, completion: @escaping (ListViewItemNodeLayout, @escaping (ListViewItemApply) -> Void) -> Void) {
-        Queue.mainQueue().async {
-            if let node = node() as? LunagramFontChoiceItemNode {
-                let makeLayout = node.asyncLayout()
-                async {
-                    let (layout, apply) = makeLayout(self, params, itemListNeighbors(item: self, topItem: previousItem as? ItemListItem, bottomItem: nextItem as? ItemListItem))
-                    Queue.mainQueue().async {
-                        completion(layout, { _ in apply() })
-                    }
-                }
-            }
-        }
-    }
-}
-
-final class LunagramFontChoiceItemNode: ListViewItemNode {
-    private let backgroundNode: ASDisplayNode
-    private let topStripeNode: ASDisplayNode
-    private let bottomStripeNode: ASDisplayNode
-    private let maskNode: ASImageNode
-    private let highlightedBackgroundNode: ASDisplayNode
-
-    private let titleNode: TextNode
-    private let checkNode: ASImageNode
-
-    init() {
-        self.backgroundNode = ASDisplayNode()
-        self.backgroundNode.isLayerBacked = true
-        self.topStripeNode = ASDisplayNode()
-        self.topStripeNode.isLayerBacked = true
-        self.bottomStripeNode = ASDisplayNode()
-        self.bottomStripeNode.isLayerBacked = true
-        self.maskNode = ASImageNode()
-        self.highlightedBackgroundNode = ASDisplayNode()
-        self.highlightedBackgroundNode.isLayerBacked = true
-
-        self.titleNode = TextNode()
-        self.titleNode.isUserInteractionEnabled = false
-        self.checkNode = ASImageNode()
-        self.checkNode.displaysAsynchronously = false
-
-        super.init(layerBacked: false)
-
-        self.addSubnode(self.titleNode)
-        self.addSubnode(self.checkNode)
-    }
-
-    override func setHighlighted(_ highlighted: Bool, at point: CGPoint, animated: Bool) {
-        super.setHighlighted(highlighted, at: point, animated: animated)
-        if highlighted {
-            if self.highlightedBackgroundNode.supernode == nil {
-                self.insertSubnode(self.highlightedBackgroundNode, aboveSubnode: self.backgroundNode)
-            }
-            self.highlightedBackgroundNode.alpha = 1.0
-        } else if self.highlightedBackgroundNode.supernode != nil {
-            self.highlightedBackgroundNode.removeFromSupernode()
-        }
-    }
-
-    func asyncLayout() -> (_ item: LunagramFontChoiceItem, _ params: ListViewItemLayoutParams, _ neighbors: ItemListNeighbors) -> (ListViewItemNodeLayout, () -> Void) {
-        let makeTitleLayout = TextNode.asyncLayout(self.titleNode)
-
-        return { item, params, neighbors in
-            let separatorHeight = UIScreenPixel
-            let leftInset = 44.0 + params.leftInset
-            let font = item.sampleFont ?? Font.regular(item.presentationData.fontSize.itemListBaseFontSize)
-
-            let (titleLayout, titleApply) = makeTitleLayout(TextNodeLayoutArguments(attributedString: NSAttributedString(string: item.title, font: font, textColor: item.presentationData.theme.list.itemPrimaryTextColor), backgroundColor: nil, maximumNumberOfLines: 1, truncationType: .end, constrainedSize: CGSize(width: params.width - leftInset - params.rightInset - 16.0, height: .greatestFiniteMagnitude), alignment: .natural, cutout: nil, insets: UIEdgeInsets()))
-
-            let contentSize = CGSize(width: params.width, height: max(44.0, titleLayout.size.height + 22.0))
-            let insets = itemListNeighborsGroupedInsets(neighbors, params)
-            let layout = ListViewItemNodeLayout(contentSize: contentSize, insets: insets)
-            let layoutSize = layout.size
-
-            return (layout, { [weak self] in
-                guard let strongSelf = self else {
-                    return
-                }
-                strongSelf.backgroundNode.backgroundColor = item.presentationData.theme.list.itemBlocksBackgroundColor
-                strongSelf.topStripeNode.backgroundColor = item.presentationData.theme.list.itemBlocksSeparatorColor
-                strongSelf.bottomStripeNode.backgroundColor = item.presentationData.theme.list.itemBlocksSeparatorColor
-                strongSelf.highlightedBackgroundNode.backgroundColor = item.presentationData.theme.list.itemHighlightedBackgroundColor
-
-                if strongSelf.backgroundNode.supernode == nil {
-                    strongSelf.insertSubnode(strongSelf.backgroundNode, at: 0)
-                }
-                if strongSelf.topStripeNode.supernode == nil {
-                    strongSelf.insertSubnode(strongSelf.topStripeNode, at: 1)
-                }
-                if strongSelf.bottomStripeNode.supernode == nil {
-                    strongSelf.insertSubnode(strongSelf.bottomStripeNode, at: 2)
-                }
-                if strongSelf.maskNode.supernode == nil {
-                    strongSelf.insertSubnode(strongSelf.maskNode, at: 3)
-                }
-                let hasCorners = itemListHasRoundedBlockLayout(params)
-                var hasTopCorners = false
-                var hasBottomCorners = false
-                switch neighbors.top {
-                case .sameSection(false):
-                    strongSelf.topStripeNode.isHidden = true
-                default:
-                    hasTopCorners = true
-                    strongSelf.topStripeNode.isHidden = hasCorners
-                }
-                let bottomStripeInset: CGFloat
-                switch neighbors.bottom {
-                case .sameSection(false):
-                    bottomStripeInset = leftInset
-                    strongSelf.bottomStripeNode.isHidden = false
-                default:
-                    bottomStripeInset = 0.0
-                    hasBottomCorners = true
-                    strongSelf.bottomStripeNode.isHidden = hasCorners
-                }
-
-                strongSelf.maskNode.image = hasCorners ? PresentationResourcesItemList.cornersImage(item.presentationData.theme, top: hasTopCorners, bottom: hasBottomCorners, glass: false) : nil
-                strongSelf.backgroundNode.frame = CGRect(origin: CGPoint(x: 0.0, y: -min(insets.top, separatorHeight)), size: CGSize(width: params.width, height: contentSize.height + min(insets.top, separatorHeight) + min(insets.bottom, separatorHeight)))
-                strongSelf.maskNode.frame = strongSelf.backgroundNode.frame.insetBy(dx: params.leftInset, dy: 0.0)
-                strongSelf.topStripeNode.frame = CGRect(origin: CGPoint(x: 0.0, y: -min(insets.top, separatorHeight)), size: CGSize(width: layoutSize.width, height: separatorHeight))
-                strongSelf.bottomStripeNode.frame = CGRect(origin: CGPoint(x: bottomStripeInset, y: contentSize.height - separatorHeight), size: CGSize(width: layoutSize.width - bottomStripeInset - params.rightInset - 16.0, height: separatorHeight))
-                strongSelf.highlightedBackgroundNode.frame = CGRect(origin: CGPoint(), size: CGSize(width: params.width, height: contentSize.height))
-
-                let _ = titleApply()
-                strongSelf.titleNode.frame = CGRect(origin: CGPoint(x: leftInset, y: floor((contentSize.height - titleLayout.size.height) / 2.0)), size: titleLayout.size)
-
-                strongSelf.checkNode.image = PresentationResourcesItemList.checkIconImage(item.presentationData.theme)
-                strongSelf.checkNode.isHidden = !item.isSelected
-                if let image = strongSelf.checkNode.image {
-                    strongSelf.checkNode.frame = CGRect(x: params.leftInset + 14.0, y: floor((contentSize.height - image.size.height) / 2.0), width: image.size.width, height: image.size.height)
-                }
-            })
-        }
-    }
-}
diff --git a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/LunagramSettingsController.swift b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/LunagramSettingsController.swift
index d7ca1958..5beabd63 100644
--- a/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/LunagramSettingsController.swift
+++ b/submodules/TelegramUI/Components/PeerInfo/PeerInfoScreen/Sources/LunagramSettingsController.swift
@@ -678,9 +678,6 @@ private let lunagramCardsDimTitles = ["Аватарка ярче", "Средне
 private let lunagramParticlesDensityLevels: [(String, Int)] = [("Редко", 20), ("Средне", 40), ("Густо", 80)]
 private let lunagramParticlesSpeedLevels: [(String, Int)] = [("Медленно", 60), ("Обычно", 100), ("Быстро", 160)]
 
-/// Строка «свой файл» в списке начертаний. Стоит сразу за готовыми.
-private let lunagramOwnFontRow = 8
-
 /// Уровни настроек своей рамки — размер, осветление, свечение.
 private let lunagramFrameSizeLevels: [(String, Int)] = [("Меньше", 110), ("Средне", 150), ("Больше", 210)]
 private let lunagramFrameBrightLevels: [(String, Int)] = [("Как есть", 100), ("Светлее", 180), ("Ярко", 260)]
@@ -751,8 +748,6 @@ private enum LunagramSettingsSection: Int32 {
     case avatarFrameBright
     case avatarFrameGlow
     case profileBackground
-    case font
-    case wording
     case liveWallpaper
     case dialogCards
     case appearance
@@ -796,7 +791,7 @@ private enum LunagramSettingsCategory: Int32, CaseIterable {
     var groups: [LunagramSettingsGroup] {
         switch self {
         case .functions:
-            return [.ghost, .spy, .messages, .privacy, .font]
+            return [.ghost, .spy, .messages, .privacy]
         case .appearance:
             // Баннер, рамка и всё про имя/статус переехали в «Изменить
             // профиль» — как на ПК. Здесь остаётся то, что видно в переписке
@@ -818,7 +813,6 @@ private enum LunagramSettingsGroup: Int32, CaseIterable {
     case liveWallpaper
     case dialogCards
     case particles
-    case font
     case avatars
 
     var title: String {
@@ -843,8 +837,6 @@ private enum LunagramSettingsGroup: Int32, CaseIterable {
             return "Список переписок"
         case .particles:
             return "Частицы"
-        case .font:
-            return "Шрифт и надписи"
         case .avatars:
             return "Аватарки"
         }
@@ -874,8 +866,6 @@ private enum LunagramSettingsGroup: Int32, CaseIterable {
             return "bubble.left"
         case .particles:
             return "face.smiling"
-        case .font:
-            return "textformat.size"
         case .avatars:
             return "person.crop.circle"
         }
@@ -904,8 +894,6 @@ private enum LunagramSettingsGroup: Int32, CaseIterable {
             return [.dialogCards]
         case .particles:
             return [.appearance, .particlesDensity, .particlesSpeed]
-        case .font:
-            return [.font, .wording]
         case .avatars:
             // Собирается своим построителем (lunagramAvatarsEntries).
             return []
@@ -930,7 +918,6 @@ private enum LunagramSettingsEntry: ItemListNodeEntry {
     // Живой образец сверху экрана: раздел, вид, слепок настроек.
     case preview(LunagramSettingsSection, LunagramPreviewKind, Int)
     // Выбор начертания: название пишется тем самым шрифтом.
-    case fontChoice(LunagramSettingsSection, Int32, String, Bool)
     case input(LunagramSettingsSection, String, String, String)
     case disclosure(LunagramSettingsSection, String)
     case editor(LunagramSettingsSection, String)
@@ -947,7 +934,7 @@ private enum LunagramSettingsEntry: ItemListNodeEntry {
             return LunagramSettingsSection.menu.rawValue
         case .ghostRow, .readRow:
             return LunagramSettingsSection.ghost.rawValue
-        case let .header(section, _, _), let .toggle(section, _, _), let .toggleDesc(section, _, _, _), let .action(section, _, _), let .dim(section, _, _, _), let .slider(section, _, _, _, _, _, _, _), let .preview(section, _, _), let .fontChoice(section, _, _, _), let .input(section, _, _, _), let .disclosure(section, _), let .editor(section, _), let .photoAction(section, _, _), let .info(section, _, _):
+        case let .header(section, _, _), let .toggle(section, _, _), let .toggleDesc(section, _, _, _), let .action(section, _, _), let .dim(section, _, _, _), let .slider(section, _, _, _, _, _, _, _), let .preview(section, _, _), let .input(section, _, _, _), let .disclosure(section, _), let .editor(section, _), let .photoAction(section, _, _), let .info(section, _, _):
             return section.rawValue
         }
     }
@@ -987,8 +974,6 @@ private enum LunagramSettingsEntry: ItemListNodeEntry {
         case let .preview(section, _, _):
             // Образец всегда самой первой строкой раздела.
             return section.rawValue * 100 - 1
-        case let .fontChoice(section, index, _, _):
-            return section.rawValue * 100 + 60 + index
         case let .input(section, _, _, _):
             return section.rawValue * 100 + 50
         case let .disclosure(section, _):
@@ -1076,12 +1061,6 @@ private enum LunagramSettingsEntry: ItemListNodeEntry {
             } else {
                 return false
             }
-        case let .fontChoice(lhsSection, lhsIndex, lhsTitle, lhsSelected):
-            if case let .fontChoice(rhsSection, rhsIndex, rhsTitle, rhsSelected) = rhs, lhsSection == rhsSection, lhsIndex == rhsIndex, lhsTitle == rhsTitle, lhsSelected == rhsSelected {
-                return true
-            } else {
-                return false
-            }
         case let .disclosure(lhsSection, lhsTitle):
             if case let .disclosure(rhsSection, rhsTitle) = rhs, lhsSection == rhsSection, lhsTitle == rhsTitle {
                 return true
@@ -1183,14 +1162,6 @@ private enum LunagramSettingsEntry: ItemListNodeEntry {
             })
         case let .preview(_, kind, token):
             return LunagramPreviewItem(presentationData: presentationData, kind: kind, token: token, sectionId: self.section)
-        case let .fontChoice(section, index, title, selected):
-            // Название рисуем тем шрифтом, который эта строка выбирает.
-            // Последняя строка — свой файл, у него свой номер начертания.
-            let styleIndex = Int(index) == lunagramOwnFontRow ? LunagramSettings.ownFontStyle : Int(index)
-            let sample = LunagramSettings.sampleFont(styleIndex: styleIndex, size: presentationData.fontSize.itemListBaseFontSize)
-            return LunagramFontChoiceItem(presentationData: presentationData, title: title, sampleFont: sample, isSelected: selected, sectionId: self.section, action: {
-                arguments.setDim(section, Int(index))
-            })
         case let .slider(_, id, title, value, minValue, maxValue, step, label):
             return LunagramSliderItem(presentationData: presentationData, title: title, value: value, minValue: minValue, maxValue: maxValue, step: step, valueText: { _ in label }, sectionId: self.section, updated: { newValue in
                 arguments.setSlider(id, newValue)
@@ -1231,10 +1202,6 @@ private struct LunagramMediaState: Equatable {
     var particlesSpeed: Int
     var avatarFrameStyle: Int
     var hasAvatarFrameImage: Bool
-    var fontTitle: String?
-    var fontStyle: Int
-    var fontHasCyrillic: Bool
-    var wordingPack: Int
     var nameColor: Int
     var nameGlow: Int
     var frameSize: Int
@@ -1291,7 +1258,7 @@ private struct LunagramMediaState: Equatable {
     }
 
     private static func legacyCurrent() -> LunagramMediaState {
-        return LunagramMediaState(wallpaperDim: LunagramSettings.liveWallpaperDim,hasWallpaper: LunagramSettings.liveWallpaperURL != nil, bannerDim: LunagramSettings.profileBannerDim, hasBanner: LunagramSettings.profileBannerURL != nil, cardsDim: LunagramSettings.dialogCardsDim, particlesKind: LunagramSettings.particlesKind, particlesDensity: LunagramSettings.particlesDensity, particlesSpeed: LunagramSettings.particlesSpeed, avatarFrameStyle: LunagramSettings.avatarFrameStyle, hasAvatarFrameImage: LunagramSettings.avatarFrameImageURL != nil, fontTitle: LunagramSettings.customFontTitle, fontStyle: LunagramSettings.fontStyle, fontHasCyrillic: LunagramSettings.customFontHasCyrillic, wordingPack: LunagramSettings.wordingPack, nameColor: LunagramSettings.profileNameColor, nameGlow: LunagramSettings.profileNameGlow, frameSize: LunagramSettings.avatarFrameScale, frameBright: LunagramSettings.avatarFrameBrightness, frameGlow: LunagramSettings.avatarFrameGlow, bannerPreset: LunagramSettings.profileBannerPreset, deviceMask: LunagramSettings.deviceMaskIndex, cardsRadius: LunagramSettings.dialogCardsRadius, cardsStroke: LunagramSettings.dialogCardsStroke, cardsMuteOpacity: LunagramSettings.dialogCardsMuteOpacity, bannerBlur: LunagramSettings.profileBannerBlur, sendWithoutSound: LunagramSettings.sendWithoutSound, sendFontStyle: LunagramSettings.sendFontStyle, hasProfileBackground: LunagramSettings.hasProfileBackground, profileBgDim: LunagramSettings.profileBackgroundDim, profileBgBlur: LunagramSettings.profileBackgroundBlur, listTextMode: LunagramSettings.listTextMode, listTextColor: LunagramSettings.listTextColor, cardsBlur: LunagramSettings.dialogCardsBlur, cardsStrokeWidth: LunagramSettings.dialogCardsStrokeWidth, particlesOpacity: LunagramSettings.particlesOpacity, particlesRotation: LunagramSettings.particlesRotation)
+        return LunagramMediaState(wallpaperDim: LunagramSettings.liveWallpaperDim,hasWallpaper: LunagramSettings.liveWallpaperURL != nil, bannerDim: LunagramSettings.profileBannerDim, hasBanner: LunagramSettings.profileBannerURL != nil, cardsDim: LunagramSettings.dialogCardsDim, particlesKind: LunagramSettings.particlesKind, particlesDensity: LunagramSettings.particlesDensity, particlesSpeed: LunagramSettings.particlesSpeed, avatarFrameStyle: LunagramSettings.avatarFrameStyle, hasAvatarFrameImage: LunagramSettings.avatarFrameImageURL != nil, nameColor: LunagramSettings.profileNameColor, nameGlow: LunagramSettings.profileNameGlow, frameSize: LunagramSettings.avatarFrameScale, frameBright: LunagramSettings.avatarFrameBrightness, frameGlow: LunagramSettings.avatarFrameGlow, bannerPreset: LunagramSettings.profileBannerPreset, deviceMask: LunagramSettings.deviceMaskIndex, cardsRadius: LunagramSettings.dialogCardsRadius, cardsStroke: LunagramSettings.dialogCardsStroke, cardsMuteOpacity: LunagramSettings.dialogCardsMuteOpacity, bannerBlur: LunagramSettings.profileBannerBlur, sendWithoutSound: LunagramSettings.sendWithoutSound, sendFontStyle: LunagramSettings.sendFontStyle, hasProfileBackground: LunagramSettings.hasProfileBackground, profileBgDim: LunagramSettings.profileBackgroundDim, profileBgBlur: LunagramSettings.profileBackgroundBlur, listTextMode: LunagramSettings.listTextMode, listTextColor: LunagramSettings.listTextColor, cardsBlur: LunagramSettings.dialogCardsBlur, cardsStrokeWidth: LunagramSettings.dialogCardsStrokeWidth, particlesOpacity: LunagramSettings.particlesOpacity, particlesRotation: LunagramSettings.particlesRotation)
     }
 }
 
@@ -1457,42 +1424,6 @@ private func lunagramSectionEntries(section: LunagramSettingsSection, values: [L
             ? "Своя рамка вместо нарисованной. Ниже — размер, осветление, свечение и искры, как на ПК. Нужна прозрачная картинка с дыркой посередине."
             : "Девять нарисованных рамок, как на ПК, или своя картинка (PNG с прозрачной серединой). Видишь рамку только ты."))
         return entries
-    case .font:
-        var entries: [LunagramSettingsEntry] = [
-            .header(.font, 0, "ШРИФТ ИНТЕРФЕЙСА"),
-            // Каждая строка написана своим шрифтом — видно, какой какой.
-            .fontChoice(.font, 0, "Системный", media.fontStyle == 0)
-        ]
-        // Готовые начертания взяты из самого айфона — скачивать нечего.
-        for style in LunagramSettings.fontStyles {
-            entries.append(.fontChoice(.font, Int32(style.id), style.title, media.fontStyle == style.id))
-        }
-        // Свой файл — последней строкой списка. Номер строки берём небольшой:
-        // строки сверяются по нему, и он обязан оставаться внутри раздела.
-        if let fontTitle = media.fontTitle {
-            entries.append(.fontChoice(.font, Int32(lunagramOwnFontRow), "Свой: \(fontTitle)", media.fontStyle == LunagramSettings.ownFontStyle))
-            entries.append(.action(.font, 30, "Заменить свой файл"))
-            entries.append(.photoAction(.font, 51, "Убрать свой файл"))
-        } else {
-            entries.append(.action(.font, 30, "Свой файл шрифта"))
-        }
-        var info = "Начертания в списке уже лежат в самом айфоне — скачивать ничего не надо, и русские буквы в каждом проверены. Шрифт встаёт на весь клиент: списки, переписки, настройки.\n\nКод и ссылки останутся ровным шрифтом — там важна одинаковая ширина букв. Перезапусти Lunagram, чтобы шрифт встал везде."
-        if media.fontTitle != nil && !media.fontHasCyrillic {
-            info += "\n\n⚠️ В твоём файле «\(media.fontTitle ?? "")» русских букв нет — с ним русские надписи станут квадратами. Это не поломка, просто в самом файле их не нарисовали."
-        }
-        entries.append(.info(.font, 0, info))
-        return entries
-    case .wording:
-        var entries: [LunagramSettingsEntry] = [.header(.wording, 0, "НАДПИСИ")]
-        // Рядом с названием — пример слов из набора, чтобы было видно, какой
-        // он: одни названия выглядели одинаково.
-        let wordingSamples = ["Переслать · Удалить", "Вжух · Снести", "Передать · Отпустить", "Телепортнуть · Сломать", "Разослать · Предать забвению"]
-        for (index, title) in LunagramSettings.wordingPackTitles.enumerated() {
-            let sample = index < wordingSamples.count ? "\(title) — \(wordingSamples[index])" : title
-            entries.append(.dim(.wording, Int32(index), sample, index == media.wordingPack))
-        }
-        entries.append(.info(.wording, 0, "Свои слова вместо телеграмовских — те самые «языки»: «Вжух» вместо «Переслать», «Предать забвению» вместо «Удалить». Меняются меню сообщения, кнопки, разделы настроек и вкладки.\n\nПереведены самые ходовые надписи, остальные остаются родными. Перезапусти Lunagram, чтобы сменилось везде."))
-        return entries
     case .avatarFrameSize:
         guard media.hasAvatarFrameImage else {
             return []
@@ -1647,36 +1578,6 @@ private func lunagramPackColor(_ color: UIColor) -> UInt32 {
     return (r << 16) | (g << 8) | b
 }
 
-/// Выбор файла шрифта в «Файлах».
-private final class LunagramFontPickerDelegate: NSObject, UIDocumentPickerDelegate {
-    private let completion: (Bool) -> Void
-
-    init(completion: @escaping (Bool) -> Void) {
-        self.completion = completion
-    }
-
-    func documentPicker(_ controller: UIDocumentPickerViewController, didPickDocumentsAt urls: [URL]) {
-        guard let url = urls.first else {
-            self.completion(false)
-            return
-        }
-        // Файл лежит в чужой папке: система пускает к нему только на время
-        // и только по явному запросу.
-        let opened = url.startAccessingSecurityScopedResource()
-        defer {
-            if opened {
-                url.stopAccessingSecurityScopedResource()
-            }
-        }
-        let imported = (try? LunagramSettings.importFont(from: url)) != nil
-        self.completion(imported)
-    }
-
-    func documentPickerWasCancelled(_ controller: UIDocumentPickerViewController) {
-        self.completion(false)
-    }
-}
-
 @available(iOS 14.0, *)
 private final class LunagramMediaPickerDelegate: NSObject, PHPickerViewControllerDelegate {
     private let allowsImages: Bool
@@ -1808,23 +1709,6 @@ private func lunagramMakeArguments(context: AccountContext, shared: LunagramShar
             (context.sharedContext.mainWindow?.viewController as? NavigationController)?.topViewController?.present(picker, animated: true, completion: nil)
             return
         }
-        if section == .font {
-            guard #available(iOS 14.0, *) else {
-                return
-            }
-            // Шрифты лежат в «Файлах», а не в галерее — и берём их оттуда.
-            let picker = UIDocumentPickerViewController(forOpeningContentTypes: [UTType.font], asCopy: true)
-            let delegate = LunagramFontPickerDelegate(completion: { imported in
-                shared.pickerHolder.delegate = nil
-                if imported {
-                    refreshMedia()
-                }
-            })
-            shared.pickerHolder.delegate = delegate
-            picker.delegate = delegate
-            (context.sharedContext.mainWindow?.viewController as? NavigationController)?.topViewController?.present(picker, animated: true, completion: nil)
-            return
-        }
         guard #available(iOS 14.0, *) else {
             return
         }
@@ -1899,12 +1783,8 @@ private func lunagramMakeArguments(context: AccountContext, shared: LunagramShar
             LunagramSettings.avatarFrameBrightness = lunagramFrameBrightLevels[max(0, min(lunagramFrameBrightLevels.count - 1, dim))].1
         case .avatarFrameGlow:
             LunagramSettings.avatarFrameGlow = lunagramFrameGlowLevels[max(0, min(lunagramFrameGlowLevels.count - 1, dim))].1
-        case .wording:
-            LunagramSettings.wordingPack = dim
         case .other:
             LunagramSettings.sendFontStyle = dim
-        case .font:
-            LunagramSettings.fontStyle = dim == lunagramOwnFontRow ? LunagramSettings.ownFontStyle : dim
         case .profileNameColor:
             // Выбрал готовый — свой из палитры больше не нужен, иначе он бы
             // перебивал выбор.
@@ -1982,11 +1862,6 @@ private func lunagramMakeArguments(context: AccountContext, shared: LunagramShar
         refreshMedia()
     }, updateText: { _, _ in
     }, photoAction: { index in
-        if index == 51 {
-            LunagramSettings.clearCustomFont()
-            refreshMedia()
-            return
-        }
         if index == 52 {
             LunagramSettings.clearProfileBackground()
             refreshMedia()
@@ -2073,8 +1948,6 @@ private func lunagramMakeArguments(context: AccountContext, shared: LunagramShar
                 LunagramSettings.setDeviceMask(index)
                 refreshMedia()
             }))
-        case .fontAndWording:
-            shared.push?(lunagramGroupController(context: context, group: .font, shared: shared))
         case .listTextMode:
             shared.push?(lunagramChoiceScreen(context: context, title: "Вид текста поверх обоев", options: lunagramListTextModeTitles, selected: LunagramSettings.listTextMode, info: "Как писать имена и отрывки в списке переписок поверх обоев. Подложка — тёмная плашка под текстом, свечение — ореол вокруг букв. Переливающийся цвет обходит радугу за десять секунд.", select: { index in
                 LunagramSettings.listTextMode = index
@@ -2264,7 +2137,6 @@ private enum LunagramChoice: Int32 {
     case sendWithoutSound = 1
     case sendFont = 2
     case deviceMask = 3
-    case fontAndWording = 4
     case listTextMode = 5
     case particlesKind = 6
     case particlesCount = 7
@@ -2602,8 +2474,6 @@ private func lunagramFunctionsEntries(values: [LunagramToggle: Bool], media: Lun
             rows.info(text)
         }
     }
-    rows.row(.value(LunagramChoice.fontAndWording.rawValue, "Шрифт и надписи", "", "textformat.size"))
-    rows.info("Шрифт всего приложения и свои надписи вместо телеграмовских — «языки».")
 
     return rows.entries
 }
